name: "Your Name"
exclude: [README.md, LICENSE, Makefile, Gemfile, Gemfile.lock, vendor]
# No CNAME, so the site is served from the GitHub Pages project URL:
# https://jbaranski.github.io/resume-template
url: 'https://jbaranski.github.io'
baseurl: '/resume-template'
markdown: kramdown
kramdown:
  input: GFM
